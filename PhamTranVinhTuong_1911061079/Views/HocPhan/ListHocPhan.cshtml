@model IEnumerable<PhamTranVinhTuong_1911061079.Models.HocPhan>
@{
    ViewBag.Title = "ListHocPhan";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>listHocphan</h2>

<table border="1px">
    <tr>
        <th>MaHP</th>
        <th>TenHP</th>
        <th>SoTinChi </th>

        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>@Html.DisplayFor(modelItem => item.MaHP)</td>
            <td>@Html.DisplayFor(modelItem => item.TenHP)</td>

            <td>@Html.DisplayFor(modelItem => item.SoTinChi)</td>

            <td>
                @Html.ActionLink("DangKy", "DangKy", new { id = item.MaHP }) |

            </td>
        </tr>
    }
</table>

